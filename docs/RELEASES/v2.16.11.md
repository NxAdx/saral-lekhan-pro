# v2.16.11

## Included
- matched Android splash handling to Expo's generated SDK 49 theme pattern
- removed the splash background from `AppTheme` to reduce the plain `#d9d7d2` handoff frame
- delayed splash hide until root navigation state is ready
- refined the `Saral ????` home wordmark without changing the Latin brand font
- restored a compact editor toolbar with slimmer active-state highlighting
- upgraded code block styling with a visible `CODE` label and stronger monospace surface

## Test Focus
1. Cold launch: native splash, second splash, blank frame
2. Home title rendering: `Saral ????`
3. Editor bottom toolbar selected-state cropping
4. Code block readability in light and dark themes