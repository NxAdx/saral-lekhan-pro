<resources xmlns:tools="http://schemas.android.com/tools">

  <!--
    Main App Theme: Used AFTER the splash screen is complete.
    Parent: DayNight.NoActionBar for full-screen React Native.
    NOTE: No windowIsTranslucent here — this was the grey flash source.
  -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:textColor">@android:color/black</item>
    <item name="android:editTextStyle">@style/ResetEditText</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="android:windowBackground">@color/splashscreen_background</item>
  </style>

  <!--
    SPLASH SCREEN THEME (Android 12+ SplashScreen API).
    This is the theme the Activity STARTS with.
    postSplashScreenTheme transitions to AppTheme once splash is done.
    This is the CORRECT pattern — exactly like 'simple-notes-sync'.
  -->
  <style name="Theme.AppTheme.Splash" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    <item name="windowSplashScreenAnimationDuration">300</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>

  <style name="ResetEditText" parent="@android:style/Widget.EditText">
    <item name="android:padding">0dp</item>
    <item name="android:textColorHint">#c8c8c8</item>
    <item name="android:textColor">@android:color/black</item>
  </style>

</resources>
